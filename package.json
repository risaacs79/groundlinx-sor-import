{
  "name": "groundlinx-sor-import",
  "version": "0.1.0",
  "private": true,
  "description": "VA-team upload page for the daily UGL SOR Extract — runs sync + work-order import via a Netlify serverless function.",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'static site — nothing to build'",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@netlify/functions": "^2.8.0",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
